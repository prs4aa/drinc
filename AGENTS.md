# Project Instructions

- Never run the Python server (`python server.py`, `uvicorn`, or any server execution command).
- Never run Gradle or any Android build/execution commands (`./gradlew`, etc.).
- Only allowed to run or build the React interface if necessary.
- Do not write code comments in any code files.
- Maintain clean modularity, folder structure, and standards.
